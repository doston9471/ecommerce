<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group small">
    <%= f.label 'Name and Surname' %>
    <%= f.text_field :cname, size: 40, class:"form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :address %>
    <%= f.text_area :address, rows: 3, cols: 40, class:"form-control" %>
  </div>

  <div class="form-group small">
    <%= f.label :telephone %>
    <%= f.text_field :telephone, size: 40, class:"form-control" %>
  </div>

  <div class="form-group small">
    <%= f.label :email %>
    <%= f.text_field :email, size: 40, class:"form-control" %>
  </div>

  <div class="form-group small">
    <%= f.submit 'Place Order' %>
  </div>
<% end %>
